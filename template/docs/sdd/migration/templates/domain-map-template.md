# Domain Map

本文档描述新项目的业务领域、SDD 模块、代码入口和主要数据对象之间的映射关系。

生成要求：

- 优先从 README、路由、Service、领域对象、数据库脚本和测试中识别模块。
- 模块边界不清晰时写入 Open Questions。
- 不得复制其他项目的模块名称和业务事实。

## 1. 领域总览

| 业务领域 | SDD 模块 | 主要职责 | 证据来源 |
| --- | --- | --- | --- |
| `{domain}` | `{module}` | `{responsibility}` | `{source}` |

## 2. 需求到模块的判断规则

| 如果需求涉及 | 必读模块 | 判断依据 |
| --- | --- | --- |
| `{需求类型}` | `{module list}` | `{依据}` |

## 3. 模块到代码入口

| SDD 模块 | Controller / Route / Handler | Service / Usecase | Repository / DAO / Mapper | 主要数据对象 |
| --- | --- | --- | --- | --- |
| `{module}` | `{entry}` | `{service}` | `{repository}` | `{data object}` |

## 4. 核心数据关系

```text
{对象关系或表关系}
```

说明：

- `{说明}`

## 5. 状态领域

| 状态类型 | 相关模块 | 说明 | 证据来源 |
| --- | --- | --- | --- |
| `{status}` | `{module}` | `{说明}` | `{source}` |

## 6. 高风险领域组合

| 需求类型 | 必须联合评估 | 原因 |
| --- | --- | --- |
| `{需求类型}` | `{module list}` | `{原因}` |

## 7. 维护规则

- 新增 SDD 模块时，必须更新领域总览、需求判断规则和模块代码入口。
- 新增重要入口、数据对象、状态或外部依赖时，必须评估是否更新本文档。
- 如果本文档与模块 `current.md` 冲突，以模块 `current.md` 为准，并修正本文档。

## 8. Open Questions

- [ ] Q: `{待确认领域或模块边界}`
