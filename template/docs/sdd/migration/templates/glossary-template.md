# Glossary

本文档统一新项目中的业务术语、技术缩写、状态名称和关键字段含义。

生成要求：

- 术语必须来自代码、README、接口文档、数据库脚本或测试。
- 无法确认含义时写入 Open Questions。
- 不得复制其他项目的业务术语。

## 1. 业务术语

| 术语 | 英文 / 代码常见命名 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- | --- |
| `{term}` | `{code name}` | `{meaning}` | `{module}` | `{source}` |

## 2. 身份与权限术语

| 术语 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- |
| `{term}` | `{meaning}` | `{module}` | `{source}` |

## 3. 数据与存储术语

| 术语 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- |
| `{term}` | `{meaning}` | `{module}` | `{source}` |

## 4. 状态术语

| 术语 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- |
| `{status}` | `{meaning}` | `{module}` | `{source}` |

## 5. 动作字段和关键字段

| 字段 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- |
| `{field}` | `{meaning}` | `{module}` | `{source}` |

## 6. 外部系统和缩写

| 术语 / 缩写 | 含义 | 相关模块 | 证据来源 |
| --- | --- | --- | --- |
| `{abbr}` | `{meaning}` | `{module}` | `{source}` |

## 7. 维护规则

- 新增核心业务术语时，必须更新本文档。
- 新增状态枚举、关键字段、外部系统简称时，必须更新本文档。
- 如果本文档与模块 `current.md` 冲突，以模块 `current.md` 为准，并修正本文档。

## 8. Open Questions

- [ ] Q: `{待确认术语或字段口径}`
