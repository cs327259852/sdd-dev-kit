# Module History: {module}

本文档记录 `{module}` 模块 feature 时间线，只追加记录。

## 1. Baseline

存量基线说明：

- 本项目引入 SDD 前已有存量代码时，早期 feature spec 可不追溯补齐。
- 存量模块当前行为以 `current.md` 为准。
- 后续变更详情以对应 feature 目录为准。

## 2. Timeline

| Date | Feature | Status | Summary | Spec | Branch | Commit |
| --- | --- | --- | --- | --- | --- | --- |
| `{yyyy-mm-dd}` | `{feature}` | `Baseline / In Progress / Released` | `{summary}` | `{spec path}` | `{branch}` | `{commit}` |

## 3. Status Values

- `Baseline`：引入 SDD 时的存量基线。
- `In Progress`：开发中。
- `Released`：已上线或已合并到目标分支。

## 4. Maintenance Rules

- 只追加记录，不改写已发布历史，除非修正明显错误。
- 不在本文档记录完整需求细节，详情链接到 feature 目录。
- 如果 feature 影响多个模块，每个受影响模块都应追加记录。

## 5. Open Questions

- [ ] Q: `{待确认历史或基线信息}`
