# Architecture

本文档描述新项目的技术架构、代码分层、关键链路和高风险基础设施。

生成要求：

- 只记录可验证事实。
- 无法确认的架构判断写入 Open Questions。
- 不得把其他项目的技术栈作为默认事实。

## 1. 项目结构

| 目录 / 模块 | 作用 | 证据来源 |
| --- | --- | --- |
| `{path}` | `{作用}` | `{README / 构建文件 / 代码入口}` |

## 2. 技术栈

| 类别 | 技术 / 框架 | 证据来源 |
| --- | --- | --- |
| 语言 | `{language}` | `{source}` |
| Web / RPC | `{framework}` | `{source}` |
| 数据库 | `{database}` | `{source}` |
| 数据访问 | `{ORM / Repository / Mapper}` | `{source}` |
| 缓存 | `{cache / 不涉及 / 未确认}` | `{source}` |
| 消息 | `{mq / 不涉及 / 未确认}` | `{source}` |
| 搜索 | `{search / 不涉及 / 未确认}` | `{source}` |
| 文件存储 | `{storage / 不涉及 / 未确认}` | `{source}` |
| 测试 | `{test framework}` | `{source}` |

## 3. 代码分层

### 3.1 `{层级名称}`

主要目录：

- `{path}`

职责：

- `{职责}`

约束：

- `{约束}`

## 4. 核心链路

### 4.1 `{链路名称}`

入口：

- `{entrypoint}`

关联模块：

- `{module}`

关键点：

- `{关键行为}`

风险：

- `{风险和边界}`

## 5. 外部依赖与状态组件

| 组件 | 用途 | 高风险点 | 证据来源 |
| --- | --- | --- | --- |
| `{component}` | `{用途}` | `{风险}` | `{source}` |

## 6. 部署和运行

| 项 | 内容 |
| --- | --- |
| 启动入口 | `{entry}` |
| 配置来源 | `{config}` |
| 环境区分 | `{env}` |
| 构建命令 | `{build}` |
| 测试命令 | `{test}` |

## 7. 维护规则

- 新增关键入口、外部依赖、分层边界或部署方式时，必须更新本文档。
- 如果本文档与代码冲突，必须停止并报告冲突。

## 8. Open Questions

- [ ] Q: `{待确认架构问题}`
