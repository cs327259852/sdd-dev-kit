# Frontend App Example

For a frontend app, `sdd-bootstrap` should inspect:

- `package.json`
- routing setup
- state management
- API clients
- component structure
- environment variable usage
- test setup

The generated SDD facts should document UI modules, API boundaries, permission
assumptions, build commands, and validation commands.

